name: Trigger CodeBuild

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  

jobs:
  trigger-codebuild:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Configure AWS credentials
      run: |
        aws configure set aws_access_key_id ASIA32WXPXTCNJVWAOD2
        aws configure set aws_secret_access_key soDLYxRuROsIT/RHszVO6knqHOrWvGs+p9TrBiGS
        aws configure set region us-east-1

    - name: Start CodeBuild
      run: |
        aws codebuild start-build --project-name GitHubActionWithcbATandT --region us-east-1
