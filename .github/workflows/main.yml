name: Trigger CodeBuild

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  trigger-codebuild:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Start CodeBuild
      run: |
        aws configure set aws_access_key_id ASIA32WXPXTCBZA4ZQGG
        aws configure set aws_secret_access_key 5ZRH7I2ip1bduxzs+3rff1AP/kaO6C+MR7WqmIc4
        aws configure set region us-east
        aws codebuild start-build --project-name GitHubActionWithcbATandT
