name: Trigger CodeBuild

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  trigger-codebuild:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Start CodeBuild
      run: |
        aws configure set aws_access_key_id ASIA32WXPXTCD56UHLRZ
        aws configure set aws_secret_access_key I/suYR8QuwFrcf4Xfuu00teexanXJQWPTurhjXWl
        aws configure set region us-east
        aws codebuild start-build --project-name GitHubActionWithcbATandT
