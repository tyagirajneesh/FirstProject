name: Trigger CodeBuild

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  trigger-codebuild:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
        
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ASIA32WXPXTCBZA4ZQGG
        aws-secret-access-key: 5ZRH7I2ip1bduxzs+3rff1AP/kaO6C+MR7WqmIc4
        aws-region: us-east-1 # Replace with your desired region

    - name: Start CodeBuild
      run: |
        aws codebuild start-build --project-name GitHubActionWithcbATandT
